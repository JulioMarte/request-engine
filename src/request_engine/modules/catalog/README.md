# Catalog module

Owns `Offering`, `OfferingVersion`, reusable offering configuration, and `ResourceRequirementTemplate` definition.

Catalog describes what can be requested. Runtime capacity commitments are owned by booking. Historical offering/version meaning must remain reconstructible after later catalog changes.
