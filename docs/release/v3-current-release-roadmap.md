# Request Engine V3 — current release roadmap

Status: **Phase 6 V3 Freeze & Release Proof closed on the current G20 candidate; release promotion remains in progress.**

Repository reference points:

- integration branch: `development`;
- frozen post-G19 candidate source: `4311200a8a9d8dfa18340c0eba5dff0cfdb47803`;
- post-G17 `development` base for G20: `489c43d4bed0060540099746a47d9abf653de300`;
- current G20 release branch: `release/v3-final-release-manifest`;
- latest fully proven G20 source head before this documentation reconciliation: `921020052833628bf1061aaa25ecd595ba2d0439`;
- canonical G20 CI for that source head: #1235 (`32292875575`);
- PostgreSQL target: PostgreSQL 18.

CI #1235 produced a complete, semantically valid release bundle with all G01–G20 `PASS`, `evidence_status: VALID`, `release_status: READY`, zero missing artifacts, zero validation errors, a clean tree, and zero test-quality errors or warnings. That evidence is authoritative only for the exact source/tested/tree identities recorded in the artifact. This documentation reconciliation changes the source tree and therefore must itself survive canonical exact-head CI before PR #71 is merge-authoritative.

`docs/release/v3-release-gates.md` remains the canonical gate registry. Historical Phase 6 planning/rebaseline documents remain useful design history, but they do not override this roadmap, the gate registry, or executable evidence.

## 1. Current gate state

| Gate range | State | Meaning |
|---|---:|---|
| G01–G16 | PASS | executable proof and registry closure exist and are regenerated by the canonical release pipeline |
| G17 | PASS | the frozen candidate and reviewed `0001_initial` are structurally, behaviorally and runtime-equivalent with explicit source/tested provenance |
| G18 | PASS | the unified attack/race/crash/retry/order/mutation suite is mandatory and semantically validated |
| G19 | PASS | fresh PostgreSQL 18 bootstrap and production-style runtime-role proof are mandatory and semantically validated |
| G20 | PASS | an independent exact-head final-release proof and validator bind the full evidence set and are required before `READY` |

Phase 6 has no remaining release gate. What remains is **promotion discipline**: merge the proven G20 change to `development`, prove the exact post-merge tree, promote `development` to `main`, and tag only an exact tree with authoritative release evidence.

## 2. Frozen candidate and `0001_initial`

The V3 candidate was frozen after G18 and G19. The freeze locks the 43-file candidate migration inventory, frozen source commit/tree, migration Git blobs, candidate apply script and canonical schema-fingerprint tool. Post-freeze descendants are accepted only when bounded Git ancestry from the frozen source is independently demonstrated; equality is not silently assumed.

G17 independently constructs two PostgreSQL 18 databases:

1. database A from the complete frozen candidate chain;
2. database B through `alembic upgrade head` using the reviewed `0001_initial` baseline.

Both construction paths must retain identical canonical structure and the same behavioral/runtime contract.

Reviewed baseline SQL SHA-256:

`502c98fcce5b5480a3e8f34804ce3a61495e679811a3ac6d0be4872107c34c88`

Canonical structural fingerprint:

`8345eec114eb4af2184c0796debece536e27d7fb4851f77811b2721df1afd877`

Canonical G17 behavioral proof: **466 tests on each construction path**, zero failures/errors/skips, with identical sorted test inventory SHA-256:

`39601a26ac608d86b86e8338ccfbbbe32d9c1d4b86769f6a5ab4230b45118b4d`

After V3 is released, `0001_initial` becomes immutable migration history. Future schema changes are append-only migrations.

## 3. G20 closure

G20 deliberately avoids a registry tautology. The release pipeline is ordered as:

```text
underlying G01–G19 evidence
        ↓
preflight manifest (structurally NOT_READY)
        ↓
independent G20 proof
        ↓
independent G20 semantic validator
        ↓
final manifest
        ↓
READY only when evidence is VALID, G20 is valid, and G01–G20 are PASS
```

The G20 proof binds at least:

- source head, integration base, tested checkout and Git tree identities;
- clean working-tree state;
- PostgreSQL/Python runtime contract;
- frozen migration-set identity;
- schema and public API fingerprints;
- reviewed `0001_initial` digest and G17 equivalence;
- invariant/race/gate registry digests;
- every mandatory evidence artifact and its digest;
- semantic validation status for the mandatory evidence set;
- canonical test inventory;
- test-quality summary cross-checked against the actual quality artifact;
- all G01–G20 gate statuses.

The independent validator requires the exact G20 criteria inventory, rejects missing criteria or lying `PASS` payloads, and rejects any test-quality warning. Concurrency evidence used as release proof must observe real PostgreSQL overlap; scheduler-delay sleeps are not accepted as the synchronization mechanism for release-critical races.

## 4. Canonical G20 evidence checkpoint

CI #1235 (`32292875575`) on source head `921020052833628bf1061aaa25ecd595ba2d0439` completed every required job successfully. Its `v3-candidate-release-proof` artifact records:

- source head `921020052833628bf1061aaa25ecd595ba2d0439`;
- integration base `489c43d4bed0060540099746a47d9abf653de300`;
- tested/checkout SHA `08d980b9367780f3c9ccc9d1d263ff7820cfd6af`;
- tree SHA `ab760634f555d727e00878244290a83aea7a4abd`;
- `working_tree_dirty: false`;
- G01–G20 `PASS`;
- G20 proof `PASS` with all 12 required criteria `true`;
- `evidence_status: VALID`;
- `release_status: READY`;
- `artifact_set_complete: true`;
- `missing_artifacts: []`;
- `validation_errors: []`;
- test-quality status `PASS`, 46 tests audited, zero errors and zero warnings;
- G17 structural and behavioral equivalence still `PASS` with 466/466 tests on each construction path.

Artifact digest for that checkpoint:

`sha256:83e76b90cccb19901554b756d4acbe5c0fb4c812446509932191fb31747d2425`

This is a provenance checkpoint, not authority for a later commit. The present documentation reconciliation must regenerate the same release contract on its own exact head.

## 5. Current execution order — release promotion

There is no further Phase 6 gate to implement. The remaining work is:

```text
canonical exact-head CI on final PR #71 head
        ↓
inspect uploaded evidence: VALID + READY + G01–G20 PASS + zero warnings
        ↓
mark PR #71 ready and merge to development
        ↓
identify exact post-merge development commit/tree
        ↓
run/confirm authoritative release evidence on that exact development tree
        ↓
open/merge development → main under repository rulesets
        ↓
verify exact main commit/tree and regenerate evidence if promotion changes it
        ↓
tag/release only the exact proven tree
```

A PR synthetic merge checkout is valid evidence for the merge scenario it tested, but it must not be mislabeled as the later literal `development` or `main` commit SHA. Promotion authority always follows the exact commit/tree recorded by the final evidence bundle.

## 6. Promotion discipline

Before PR #71 may merge to `development`:

1. canonical CI must pass on its exact final source head;
2. the uploaded release bundle must be inspected, not inferred from green job status;
3. G01–G20 must all be `PASS`;
4. the final manifest must be `VALID`, complete, clean and `READY`;
5. G20 semantic validation must be `PASS`;
6. test-quality errors and warnings must both be zero;
7. the branch must be mergeable and review conversations resolved.

After merge to `development`, the actual integration SHA/tree becomes the next authority target. Before promotion to `main`, canonical evidence must be valid for the exact development tree that will be promoted. If merge/promotion creates a different executable tree, evidence must be regenerated rather than inherited from a historical PR artifact.

## 7. Explicit non-goals during release promotion

Do not add unrelated product breadth while promoting V3. In particular, release work must not grow Request Engine into a CRM/ERP/accounting/PSP/PBX, universal workflow engine, workforce optimizer, GPS/delivery platform, generic agent framework, advanced payment domain or generalized capacity-pool/federation product.

Correctness fixes exposed by release evidence are allowed. New feature scope belongs after the V3 release unless a narrowly scoped change is required to make an existing release guarantee correct.

## 8. Definition of done

### Phase 6 Freeze & Release Proof

Closed when one exact candidate has all of the following together:

- G01–G20 `PASS`;
- `V3-I01..V3-I66` reconciled to executable evidence;
- every release-critical R01–R29 race `PASS` with deterministic evidence;
- final `0001_initial` structurally, behaviorally and runtime-equivalent to the frozen candidate;
- production-like bootstrap through real app/worker/admin runtime roles;
- frozen public API/capability/error contract;
- representative query-plan evidence;
- no P0/P1 blocker;
- exact-head evidence `VALID`, complete and clean;
- independent G20 proof valid for the same source/tested/tree identity;
- zero test-quality errors and warnings;
- final manifest `READY`.

CI #1235 demonstrated this state for its exact candidate. The final documentation head must now reproduce it.

### V3 release

V3 is released only after the G20 change is integrated, the exact promoted `development`/`main` tree has authoritative evidence, repository rulesets are satisfied, and the exact proven `main` tree is tagged/released. Until then, the V3 core is **release-ready candidate software**, not yet a completed `main` release.
