# Request Engine historical archive

This directory contains documentation from retired Request Engine architectures and implementation experiments.

It is intentionally isolated from the current design so old Convex/Chatwoot/Vite/vertical-specific decisions cannot be mistaken for current requirements.

See `AGENTS.md` in this directory: **the archive is no-touch unless the user explicitly requests changes to historical material.**

Current authoritative documentation lives one directory above.
