"""Pre-production F2 SQL steps preserved behind one Alembic revision.

These modules are not Alembic revisions. The consolidated 0004 migration executes
them in their historically proven order so schema semantics stay byte-for-byte
aligned with the green pre-consolidation evidence.
"""
