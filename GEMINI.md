@./AGENTS.md

# Gemini CLI note

Treat the imported `AGENTS.md` as the repository-wide operating contract. When working under `docs/`, `migrations/`, `src/request_engine/modules/`, or `tests/`, also load the nearest local `GEMINI.md`, which imports that area's stricter `AGENTS.md`.
